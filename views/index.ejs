<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nyaya-Setu | Add Evidence</title>
  <style>
    :root {
      --primary-color: #1e3a8a; /* Deep Navy */
      --secondary-color: #64748b; /* Slate */
      --accent-color: #2563eb; /* Law Blue */
      --bg-color: #f8fafc;
      --card-bg: #ffffff;
      --text-main: #1e293b;
    }

    body {
      font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background-color: var(--bg-color);
      color: var(--text-main);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .container {
      background: var(--card-bg);
      padding: 2.5rem;
      border-radius: 12px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05), 0 5px 10px rgba(0, 0, 0, 0.02);
      width: 100%;
      max-width: 450px;
      border-top: 5px solid var(--primary-color);
    }

    h1 {
      margin-top: 0;
      font-size: 1.5rem;
      color: var(--primary-color);
      text-align: center;
      letter-spacing: -0.5px;
    }

    p.subtitle {
      text-align: center;
      color: var(--secondary-color);
      font-size: 0.9rem;
      margin-bottom: 2rem;
    }

    .form-group {
      margin-bottom: 1.25rem;
    }

    label {
      display: block;
      font-size: 0.85rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--secondary-color);
    }

    input[type="text"],
    input[type="file"] {
      width: 100%;
      padding: 0.75rem;
      border: 1.5px solid #e2e8f0;
      border-radius: 6px;
      box-sizing: border-box;
      transition: border-color 0.2s;
    }

    input:focus {
      outline: none;
      border-color: var(--accent-color);
      ring: 2px solid rgba(37, 99, 235, 0.1);
    }

    /* Custom File Upload Styling */
    input[type="file"] {
      background: #f1f5f9;
      cursor: pointer;
    }

    button {
      width: 100%;
      background-color: var(--primary-color);
      color: white;
      padding: 0.8rem;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s ease;
      margin-top: 1rem;
    }

    button:hover {
      background-color: var(--accent-color);
    }

    .footer-links {
      margin-top: 1.5rem;
      text-align: center;
      border-top: 1px solid #edf2f7;
      padding-top: 1rem;
    }

    .footer-links a {
      color: var(--accent-color);
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: 500;
    }

    .footer-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Nyaya-Setu</h1>
    <p class="subtitle">Evidence Management System</p>
    
    <form action="/evidence" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="caseId">Case Reference Number</label>
        <input type="text" id="caseId" name="caseId" placeholder="e.g. CAS-2024-001" required>
      </div>

      <div class="form-group">
        <label for="officerId">Investigating Officer ID</label>
        <input type="text" id="officerId" name="officerId" placeholder="Badge or ID Number" required>
      </div>

      <div class="form-group">
        <label for="file">Digital Evidence File</label>
        <input type="file" id="file" name="file" required>
      </div>

      <button type="submit">Securely Upload Evidence</button>
    </form>

    <div class="footer-links">
      <a href="/evidence">← View All Logged Evidence</a>
    </div>
  </div>

</body>
</html>